# API

В этой папке хранится канонический API-контракт платформы SDK.

Точка входа:

- [openapi/max-bot-api.yaml](C:\pro\max\api\openapi\max-bot-api.yaml)

Принцип:

- сначала меняется контракт
- потом codegen
- потом SDK и документация
